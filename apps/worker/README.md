# OmniScope Worker (Skeleton)

This is the internal v1 worker skeleton for background jobs.

## Run (dev)

1) Install dependencies.
2) Start the worker:

```
python -m app.worker
```

## Notes
- Jobs are stubbed for Sprint 2 Phase 3.
- Redis is required for the queue connection.
